# Pinned exactly. The checkpoints were written by nnunetv2 2.6.2 under torch
# 2.6.0; loading them under a different nnU-Net or a different
# dynamic-network-architectures is not guaranteed to reconstruct the same
# network, and torch.load's weights_only semantics changed across these
# versions. Do not float any of these.
nnunetv2==2.6.2
dynamic-network-architectures==0.4.3
acvl-utils==0.2.6
batchgenerators==0.25.1
batchgeneratorsv2==0.3.0
SimpleITK==2.5.5
nibabel==5.4.2
numpy<2.1
scipy<1.14
scikit-image
pandas
seaborn
tqdm
